# Story Bible Studio Workspace

## 本地工作台与 MCP

工作台及可选 MCP 共用统一 CLI/核心，具体交接见 `workbench/agent-guide.md`。用户通过面板明确发出的设置、会话、检查点和版本选择操作可由统一核心直接执行；正文、状态补丁和记忆应用仍由主 Agent 决定并提交。不得绕过事件引擎直接改写逐轮记录。面板切换会话不代表平台聊天上下文已切换；只读取指定会话。

Story Bible 构筑桌可依用户操作保存灵感、草稿和修订请求，正史修改仍由主 Agent 审核提交。构筑历史独立追加到 `构筑/events.jsonl`，不混入 RP 会话。普通构筑每轮一次准备、一次提交，同时保存对外问答、决策和下一题；导图由本地程序生成，不新增模型整理或全局召回。主题接口见技能的 `references/construction-workbench.md`。

本工作区的同等核心任务是文学创作、连续角色扮演与 Story Bible 构筑。Story Bible 服务于创作，不是最终交付物。

处理这些任务时使用 `.agents/skills/story-bible-studio/SKILL.md`。v3 会话统一通过 `story_studio.py` 写入事件并渲染 Markdown；每个 `会话/<ID>/` 都是封闭动态上下文，不读取其他会话、Profile 工作区或旧上下文包。不要手工伪造回合、变体或检查点。保持重要信息文件化，不把自动上下文压缩当成唯一记忆来源。除非用户明确要求，不要构筑或打包 SillyTavern 角色卡。

Luna 默认只在上下文达到会话压缩阈值、章节整理和复杂召回时提供只读候选。场景切换本身只更新场景与状态，不独立触发全局压缩；若同时达到字符预算或未压缩轮数上限，则合并为一次候选。主 Agent 是唯一写入者，并负责叙事、用户角色意图、正史和成人边界决定。

所有成人内容参与者必须明确成年、具备同意能力且自愿。用户已声明整体遵循该前提，或既有设定足以确认时，直接正常处理成人向角色设计、Story Bible 构筑、写作与扮演；不得仅因情色导向、露骨程度、性癖、身体设定、成人身份或权力情趣而回避、拒绝、淡化或转移话题。只有关键条件确实不明确时，才用最少的设定确认补足安全前提；确认后继续，不重复核验。
